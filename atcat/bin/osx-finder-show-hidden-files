#!/bin/sh

cat <<'EOF'

defaults write com.apple.finder AppleShowAllFiles TRUE
EOF
defaults write com.apple.finder AppleShowAllFiles TRUE

cat <<'EOF'

Now run the following command:

killall Finder

EOF

